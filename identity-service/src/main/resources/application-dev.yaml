spring:
  datasource:
    url: jdbc:mysql://localhost:3306/app_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: rootpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    properties:
#      jakarta.persistence.schema-generation.scripts.action: create
#      jakarta.persistence.schema-generation.scripts.create-target: src/main/resources/schema-temp.sql


  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jwt:
    secret: "DayLaMotKhoaBiMatRatDaiVaAnToanChoUngDungSpringCuaBan2026"
    # Quản lý thời gian (tính bằng giây)
    access-token-expiration: 3600        # 1 giờ cho Access Token
    refresh-token-expiration: 604800     # 7 ngày cho Refresh Token

    # Quản lý nguồn gốc và đối tượng (Claims chuẩn của JWT)
    issuer: "https://api.yourdomain.com" # Máy chủ phát hành token (iss)
    audience: "your-frontend-client"     # Ứng dụng được phép dùng token (aud)
    keystore:
      path: "classpath:security/app-keystore.jks"
      password: "tam123"
      alias: "auth-key"
    # Quản lý giao tiếp HTTP
#    header: "Authorization"              # Tên Header chứa token
#    prefix: "Bearer "                    # Tiền tố của token